<Window x:Class="arsX.Sol_Explorer.ManageCredentialsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Credentials"
        Height="260" Width="400">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Site Name -->
        <TextBlock Text="Site:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBlock x:Name="SiteNameTextBlock" Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,8"/>

        <!-- Site URL -->
        <TextBlock Text="URL:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBlock x:Name="SiteUrlTextBlock" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" Margin="0,0,0,8"/>

        <!-- Username -->
        <TextBlock Text="Username:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="UsernameTextBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Password -->
        <TextBlock Text="Password:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,8"/>
        <TextBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button x:Name="SaveButton" Content="Save" Width="70" Margin="0,0,8,0"/>
            <Button x:Name="ClearButton" Content="Clear" Width="70" Margin="0,0,8,0"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="70"/>
        </StackPanel>
    </Grid>
</Window>
