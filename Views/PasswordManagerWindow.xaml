<UserControl x:Class="arsX.Sol_Explorer.Views.PasswordManagerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="400" Width="600">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="CredentialsGrid" Grid.Row="0" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Site" Binding="{Binding SiteName}" Width="*"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button x:Name="AddButton" Content="Add..." Width="80" Margin="0,0,8,0"/>
            <Button x:Name="EditButton" Content="Edit..." Width="80" Margin="0,0,8,0"/>
            <Button x:Name="DeleteButton" Content="Delete" Width="80" Margin="0,0,8,0"/>
            <Button x:Name="CloseButton" Content="Close" Width="80"/>
        </StackPanel>
    </Grid>
</UserControl>
